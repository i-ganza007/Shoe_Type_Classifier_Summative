# Minimal requirements for Render deployment
# Use this if the main requirements.txt fails

# Build tools
setuptools>=65.0.0
wheel>=0.38.0

# Core web framework
fastapi==0.100.0
uvicorn==0.23.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Core ML (minimal versions)
tensorflow
numpy>=1.21.0,<1.25.0
pillow>=9.0.0,<11.0.0

# Supabase (essential only)
supabase==1.0.0

# Basic utilities
requests>=2.28.0
pydantic>=1.10.0
